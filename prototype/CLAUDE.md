TODO
- Graph for spendings
- Use the new UI from Claude Design


Notes
- For all backend changes make sure to run dotnet ef migrations add <migration_name> and dotnet ef database update
- On a new device make sure all the dependencies are installed
- Default to npm run web so I can view the app on the browser
